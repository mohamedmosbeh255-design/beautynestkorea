// Unlisted staff entry point. NOT linked anywhere on the public site,
// NOT in sitemap.xml, NO robots meta — discoverable only by staff who
// are told the URL. Both this page and /admin/login require an
// authenticated session (see src/middleware.ts); unauthenticated hits
// are bounced to / and logged.
export const ADMIN_ENTRY_PATH = "/beautynest-entry-2026-sable-heron-47";
